{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build current",
      "type": "shell",
      "command": "make",
      "args": [
        "TARGET=${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
